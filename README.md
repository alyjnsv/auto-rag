# auto-rag

[![Python 3.8+](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![LEANN](https://img.shields.io/badge/Vector%20DB-LEANN-green.svg)](https://github.com/yichuan-w/LEANN)

Автоматическая система RAG-ингестинга Markdown-документации с интеллектуальным тегированием и загрузкой в локальную векторную базу данных LEANN.

## 🎯 Особенности

- 🔍 **Рекурсивная обработка** всех Markdown файлов в директории
- 📄 **Автоматическая генерация YAML метаданных** из структуры документов
- 🏷️ **Извлечение тегов** из заголовков второго уровня (`##`)
- 📦 **Умный чанкинг** по главам с сохранением контекста
- 🗄️ **Интеграция с LEANN** - локальная векторная БД с экономией памяти до 97%
- 📊 **Подробное логирование** и JSON отчёты
- 🛡️ **Устойчивость к ошибкам** - продолжает работу при сбоях отдельных файлов

## 🚀 Быстрый старт

### Установка

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/auto-rag.git
cd auto-rag

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt

# Установите LEANN (рекомендуется через uv)
pip install uv
uv pip install leann
```

### Настройка

```bash
# Скопируйте шаблон переменных окружения
cp env_template .env

# Отредактируйте .env (опционально, для эмбеддингов через OpenAI)
# OPENAI_API_KEY=your_key_here
# OPENROUTER_API_KEY=your_key_here
```

**Примечание:** LEANN работает полностью локально и не требует API ключей. Ключи OpenAI/OpenRouter нужны только для генерации эмбеддингов через внешние API.

### Использование

```bash
# Базовый запуск
python main.py

# С параметрами
python main.py --docs-path ./my-docs --index-name my-index --dry-run
```

## 📖 Формат документов

### Структура Markdown

Система обрабатывает Markdown файлы со следующей структурой:

```markdown
# Заголовок документа

## Глава 1
Содержание первой главы...

## Глава 2
Содержание второй главы...
```

### Автоматическая генерация YAML

Для каждого Markdown файла система **автоматически создаёт** соответствующий YAML файл с метаданными:

```
docs/
├── my_document.md
└── my_document.yaml  (создаётся автоматически)
```

**Содержимое `my_document.yaml`:**
```yaml
doc_id: my_document
tags:
  - Глава 1
  - Глава 2
```

**Параметры:**
- `doc_id` - Идентификатор документа (извлекается из имени файла)
- `tags` - Список тегов (автоматически извлекаются из заголовков `##`)

## 💻 CLI параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--docs-path` | Путь к папке с документацией | `docs` |
| `--dry-run` | Не загружать в LEANN, только парсинг | `False` |
| `--report-file` | Имя JSON файла отчёта | `report.json` |
| `--index-name` | Имя индекса LEANN | `doc_id` первого документа |
| `--no-auto-yaml` | Отключить автоматическую генерацию YAML | `False` |
| `--overwrite-yaml` | Перезаписывать существующие YAML файлы | `False` |

### Примеры использования

```bash
# Обработка с указанием пути
python main.py --docs-path ./documentation

# Тестовый запуск без загрузки в БД
python main.py --dry-run

# Создание индекса с именем
python main.py --index-name production-docs

# Перезапись существующих YAML файлов
python main.py --overwrite-yaml

# Комбинация параметров
python main.py \
  --docs-path ./docs \
  --index-name my-docs \
  --report-file processing_report.json \
  --dry-run
```

## 🏗️ Архитектура

```
auto-rag/
├── main.py              # CLI точка входа
├── rag_utils.py         # Парсинг Markdown, чанкинг, генерация YAML
├── embedder.py          # Генерация эмбеддингов (OpenAI/OpenRouter)
├── vector_store.py      # Интеграция с LEANN
├── docs/                # Папка с документацией
├── logs/                # Логи работы
└── .leann/indexes/      # LEANN индексы (создаётся автоматически)
```

## 🔧 Принципы работы

1. **Парсинг документов** - Рекурсивный поиск всех `.md` файлов
2. **Извлечение метаданных** - Загрузка или генерация YAML файлов
3. **Чанкинг** - Разбиение документов по заголовкам `##`
4. **Генерация эмбеддингов** - Создание векторных представлений (опционально)
5. **Загрузка в LEANN** - Сохранение в локальную векторную БД

## 📊 Пример отчёта

После обработки создаётся JSON отчёт:

```json
{
  "processed": [
    {
      "file": "docs/my_document.md",
      "doc_id": "my_document",
      "tags": ["Глава 1", "Глава 2"],
      "chunks": 3
    }
  ],
  "errors": []
}
```

## 🎯 Преимущества LEANN

- ✅ **97% экономия хранилища** - графовая структура вместо полных эмбеддингов
- ✅ **100% приватность** - полностью локальная работа
- ✅ **Бесплатно** - open-source решение
- ✅ **Быстрый поиск** - selective recomputation
- ✅ **Поддержка метаданных** - фильтрация по тегам

## 🛠️ Разработка

### Структура кода

- `rag_utils.py` - Основная логика обработки документов
- `embedder.py` - Генерация эмбеддингов через API
- `vector_store.py` - Работа с LEANN векторной БД
- `main.py` - CLI интерфейс

### Расширение функциональности

Система легко расширяется для:
- Поддержки других форматов (PDF, DOCX, HTML)
- Изменения стратегии чанкинга
- Добавления новых полей метаданных
- Интеграции с другими векторными БД

Подробнее см. [DOCS.md](DOCS.md)

## 📝 Логирование

Все операции логируются в `logs/auto_rag.log`:

```
2024-01-15 10:30:45 INFO Обработка папки docs/
2024-01-15 10:30:46 INFO Обработано чанков: 7
2024-01-15 10:30:47 INFO ✅ Создан YAML файл: docs/my_document.yaml
2024-01-15 10:30:50 INFO ✅ LEANN индекс создан: .leann/indexes/my_document
```

## 🔒 Безопасность

- ⚠️ **Никогда не коммитьте `.env` файл**
- Используйте `.gitignore` для исключения:
  - `.env`
  - `logs/`
  - `.leann/`
- Храните API ключи в переменных окружения

## 📚 Дополнительная документация

- [INSTALL_LEANN.md](INSTALL_LEANN.md) - Подробные инструкции по установке LEANN
- [LEANN_INTEGRATION.md](LEANN_INTEGRATION.md) - Детали интеграции с LEANN
- [DOCS.md](DOCS.md) - Техническая документация для разработчиков

## 🤝 Вклад в проект

Вклад приветствуется! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 🎯 Roadmap

- [ ] Интеграция LLM для интеллектуального тегирования
- [ ] Поддержка PDF и DOCX форматов
- [ ] Веб-интерфейс для мониторинга
- [ ] Инкрементальная обработка (только изменённые файлы)
- [ ] Unit и интеграционные тесты
- [ ] CI/CD pipeline

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

## 🙏 Благодарности

- [LEANN](https://github.com/yichuan-w/LEANN) - за отличную локальную векторную БД
- OpenAI - за API эмбеддингов

---

**Сделано с ❤️ для автоматизации RAG-ингестинга**
