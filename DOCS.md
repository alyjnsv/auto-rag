# Техническая документация auto-rag

Подробное описание архитектуры, API и внутренней работы системы.

## 📚 Содержание

1. [Архитектура системы](#архитектура-системы)
2. [Детальное описание модулей](#детальное-описание-модулей)
3. [Поток данных](#поток-данных)
4. [API Reference](#api-reference)
5. [Конфигурация](#конфигурация)
6. [Расширение системы](#расширение-системы)

## 🏗️ Архитектура системы

### Компоненты

```
┌─────────────┐
│   main.py   │  ← CLI точка входа, оркестрация
└──────┬──────┘
       │
       ├──────────────┐
       │              │
┌──────▼──────┐  ┌───▼──────────────┐
│ rag_utils.py│  │pinecone_uploader │
│             │  │                  │
│ - Парсинг   │  │ - Загрузка       │
│ - Чанкинг   │  │ - Батчинг        │
│ - Теги      │  └──────┬───────────┘
└──────┬──────┘         │
       │                │
       │         ┌──────▼──────┐
       │         │ embedder.py│
       │         │             │
       │         │ - OpenAI API│
       │         │ - Embeddings│
       └─────────┴─────────────┘
```

### Зависимости

```
main.py
├── rag_utils.py (парсинг, чанкинг)
├── pinecone_uploader.py (загрузка)
│   └── embedder.py (эмбеддинги)
└── typer (CLI)
```

## 📦 Детальное описание модулей

### main.py

**Назначение:** Точка входа, CLI интерфейс, оркестрация всего pipeline.

**Основные функции:**

- `run()` - Главная функция CLI
  - Загружает переменные окружения
  - Проверяет существование папки docs
  - Вызывает обработку документов
  - Управляет dry-run режимом
  - Генерирует отчёт

**Логирование:**
- Настройка в `logging.basicConfig()`
- Файл: `logs/auto_rag.log`
- Уровень: INFO

**Обработка ошибок:**
- Try/except блоки для каждого этапа
- Ошибки логируются и не прерывают выполнение

### rag_utils.py

**Назначение:** Парсинг Markdown, извлечение метаданных, чанкинг.

**Функции:**

#### `load_yaml_metadata(md_file_path: Path) -> Dict`

Загружает YAML метаданные из отдельного файла.

**Алгоритм:**
1. Ищет файл с тем же именем, но расширением `.yaml`
2. Если не найден, ищет файл с расширением `.yml`
3. Парсит YAML содержимое
4. Возвращает словарь с метаданными или пустой словарь, если файл не найден

**Пример:**
```python
from pathlib import Path

md_file = Path("docs/my_document.md")
metadata = load_yaml_metadata(md_file)
# Для docs/my_document.md ищет docs/my_document.yaml или docs/my_document.yml
# metadata = {"doc_id": "my_document", "tags": ["Chapter1", "Chapter2"]}
# Если файл не найден, возвращает {}
```

#### `generate_yaml_metadata(md_file_path: Path, doc_id: str, tags: List[str], overwrite: bool = False) -> Path`

Автоматически генерирует YAML файл с метаданными документа.

**Алгоритм:**
1. Формирует путь к YAML файлу (`.yaml` расширение)
2. Проверяет существование файла и флаг `overwrite`
3. Создаёт словарь с метаданными (`doc_id`, `tags`)
4. Записывает YAML файл с правильным форматированием
5. Возвращает путь к созданному файлу

**Параметры:**
- `md_file_path` - Путь к Markdown файлу
- `doc_id` - Идентификатор документа
- `tags` - Список тегов
- `overwrite` - Перезаписать существующий файл (по умолчанию False)

**Пример:**
```python
from pathlib import Path

md_file = Path("docs/my_document.md")
yaml_path = generate_yaml_metadata(
    md_file, 
    doc_id="my_document",
    tags=["Chapter1", "Chapter2", "Chapter3"],
    overwrite=False
)
# Создаёт docs/my_document.yaml с метаданными
```

#### `split_to_chunks(content: str) -> List[str]`

Разбивает Markdown на чанки по заголовкам `##`.

**Алгоритм:**
1. Разбивает контент на строки
2. Ищет строки, начинающиеся с `## `
3. Группирует строки между заголовками
4. Возвращает список чанков

**Особенности:**
- Каждый чанк начинается с заголовка `##`
- Пустые чанки отфильтровываются
- Сохраняется структура Markdown

#### `parse_all_headers(content: str) -> List[str]`

Извлекает все заголовки второго уровня как список тегов.

**Возвращает:**
- Список строк (названия глав без `## `)

#### `tag_chunks(chunks: List[str], tags: List[str]) -> List[str]`

**Текущая реализация:** Заглушка, возвращает первый тег.

**TODO:** Интеграция с LLM для интеллектуального тегирования.

#### `process_markdown_docs(docs_dir: Path, auto_generate_yaml: bool = True, overwrite_yaml: bool = False)`

Главная функция обработки всех документов.

**Параметры:**
- `docs_dir` - Директория с Markdown файлами
- `auto_generate_yaml` - Автоматически генерировать YAML файлы (по умолчанию True)
- `overwrite_yaml` - Перезаписывать существующие YAML файлы (по умолчанию False)

**Алгоритм:**
1. Рекурсивно находит все `.md` файлы
2. Для каждого файла:
   - Читает содержимое Markdown
   - Загружает YAML метаданные из отдельного файла (`.yaml` или `.yml`)
   - Извлекает `doc_id` из метаданных или использует имя файла
   - Извлекает теги из метаданных или парсит заголовки `##`
   - **Автоматически генерирует YAML файл** (если `auto_generate_yaml=True` и файла нет или нужно обновить)
   - Разбивает на чанки по заголовкам `##`
   - Тегирует чанки
   - Формирует объекты чанков с метаданными
3. Собирает отчёт об обработке
4. Возвращает список чанков и отчёт

**Структура чанка:**
```python
{
    "text": "## Chapter1\nContent...",
    "metadata": {
        "doc_id": "document_name",
        "tag": "Chapter1",
        "tags": ["Chapter1", "Chapter2", ...],
        "path": "docs/document.md"
    }
}
```

### embedder.py

**Назначение:** Генерация эмбеддингов для текстовых чанков.

**Функции:**

#### `embed_chunk(text: str, model: str = "text-embedding-3-large") -> List[float]`

Генерирует векторное представление текста.

**Параметры:**
- `text` - Текст для эмбеддинга
- `model` - Модель OpenAI (по умолчанию: `text-embedding-3-large`)

**Возвращает:**
- Список float значений (1024 измерения для text-embedding-3-large)

**Обработка ошибок:**
- При отсутствии API ключа: возвращает заглушку
- При ошибке API: логирует и возвращает нулевой вектор

**Заглушка:**
```python
# Если нет API ключа
return [float(hash(text) % 1000) / 1000.0] * 10
```

### pinecone_uploader.py

**Назначение:** Загрузка чанков в векторную базу Pinecone.

**Функции:**

#### `upload_chunks(chunks, meta_report)`

Загружает чанки в Pinecone с эмбеддингами и метаданными.

**Алгоритм:**
1. Проверяет наличие всех ключей Pinecone
2. Инициализирует подключение
3. Для каждого чанка:
   - Генерирует эмбеддинг
   - Формирует ID (doc_id + hash текста)
   - Подготавливает метаданные
4. Загружает батчами (по 100 чанков)
5. Логирует прогресс

**Структура вектора для Pinecone:**
```python
{
    'id': 'doc_id-123456',
    'values': [0.1, 0.2, ...],  # 1024 измерения
    'metadata': {
        'doc_id': 'document',
        'tag': 'Chapter1',
        'tags': ['Chapter1', 'Chapter2'],
        'scope': '...',
        'owner': '...'
    }
}
```

**Обработка ошибок:**
- При отсутствии пакета: пропускает загрузку
- При отсутствии ключей: dry-run режим
- При ошибке API: логирует и продолжает

## 🔄 Поток данных

### Полный pipeline

```
1. Запуск main.py
   │
   ├─► Загрузка .env
   │
   ├─► Проверка папки docs/
   │
   ├─► process_markdown_docs()
   │   │
   │   ├─► Рекурсивный поиск .md файлов
   │   │
   │   ├─► Для каждого файла:
   │   │   │
   │   │   ├─► Чтение Markdown файла
   │   │   │   └─► Контент документа
   │   │   │
   │   │   ├─► load_yaml_metadata()
   │   │   │   └─► Метадата из .yaml/.yml файла (если есть)
   │   │   │
   │   │   ├─► parse_all_headers()
   │   │   │   └─► Список тегов из заголовков
   │   │   │
   │   │   ├─► generate_yaml_metadata() (если auto_generate_yaml=True)
   │   │   │   └─► Автоматическое создание/обновление YAML файла
   │   │   │
   │   │   ├─► split_to_chunks()
   │   │   │   └─► Список чанков
   │   │   │
   │   │   ├─► tag_chunks()
   │   │   │   └─► Теги для каждого чанка
   │   │   │
   │   │   └─► Формирование объектов чанков
   │   │
   │   └─► Отчёт об обработке
   │
   ├─► upload_chunks() (если не dry-run)
   │   │
   │   ├─► Для каждого чанка:
   │   │   │
   │   │   ├─► embed_chunk()
   │   │   │   └─► Вектор эмбеддинга
   │   │   │
   │   │   └─► Подготовка для Pinecone
   │   │
   │   └─► Батчевая загрузка в Pinecone
   │
   └─► Генерация JSON отчёта
```

### Обработка одного документа

```
Markdown файл (my_document.md)
    │
    ├─► YAML файл (my_document.yaml или my_document.yml)
    │   └─► Метадата: doc_id, tags (опционально)
    │
    ├─► Контент Markdown
    │   │
    │   ├─► Парсинг заголовков ##
    │   │   └─► Список тегов (если не указаны в YAML)
    │   │
    │   └─► Разбиение на чанки
    │       │
    │       └─► Чанк 1, Чанк 2, ...
    │
    └─► Тегирование чанков
        │
        └─► Чанк + тег
```

## 📖 API Reference

### rag_utils

```python
def load_yaml_metadata(md_file_path: Path) -> Dict
    """Загружает YAML метаданные из отдельного файла (.yaml или .yml)."""

def generate_yaml_metadata(md_file_path: Path, doc_id: str, tags: List[str], overwrite: bool = False) -> Path
    """Автоматически генерирует YAML файл с метаданными документа."""

def split_to_chunks(content: str) -> List[str]
    """Разбивает Markdown на чанки по заголовкам ##."""

def parse_all_headers(content: str) -> List[str]
    """Извлекает все заголовки второго уровня."""

def tag_chunks(chunks: List[str], tags: List[str]) -> List[str]
    """Тегирует чанки (заглушка, требует LLM интеграции)."""

def process_markdown_docs(docs_dir: Path, auto_generate_yaml: bool = True, overwrite_yaml: bool = False) -> Tuple[List[dict], dict]
    """Обрабатывает все Markdown файлы в директории."""
```

### embedder

```python
def embed_chunk(text: str, model: str = "text-embedding-3-large") -> List[float]
    """Генерирует эмбеддинг для текста."""
```

### pinecone_uploader

```python
def upload_chunks(chunks: List[dict], meta_report: dict) -> None
    """Загружает чанки в Pinecone."""
```

## ⚙️ Конфигурация

### Переменные окружения (.env)

| Переменная | Описание | Обязательная |
|-----------|----------|--------------|
| `OPENROUTER_API_KEY` | Ключ для OpenRouter API | Нет* |
| `OPENAI_API_KEY` | Ключ для OpenAI API | Нет* |
| `PINECONE_API_KEY` | Ключ для Pinecone | Нет** |
| `PINECONE_ENV` | Окружение Pinecone | Нет** |
| `PINECONE_INDEX` | Имя индекса Pinecone | Нет** |

\* Нужен хотя бы один для эмбеддингов  
\** Нужны все для загрузки в Pinecone

### Параметры CLI

```bash
python main.py [OPTIONS]

Options:
  --docs-path TEXT     Путь к папке с документацией [default: docs]
  --dry-run           Не загружать в Pinecone [default: False]
  --report-file TEXT   Имя файла отчёта [default: report.json]
  --help              Показать справку
```

## 🔧 Расширение системы

### Добавление нового формата документов

1. Создайте функцию парсера в `rag_utils.py`:

```python
def parse_pdf(file_path: Path) -> Tuple[dict, str]:
    """Парсит PDF файл."""
    # Реализация парсинга PDF
    pass
```

2. Модифицируйте `process_markdown_docs()`:

```python
def process_docs(docs_dir: Path):
    # Обработка .md
    md_files = list(docs_dir.rglob('*.md'))
    # Обработка .pdf
    pdf_files = list(docs_dir.rglob('*.pdf'))
    # ...
```

### Изменение стратегии чанкинга

Модифицируйте `split_to_chunks()`:

```python
def split_to_chunks_by_size(content: str, chunk_size: int = 1000) -> List[str]:
    """Чанкинг по размеру (в символах)."""
    # Реализация
    pass
```

### Интеграция другой LLM

Создайте новый модуль `llm_tagger.py`:

```python
def tag_with_llm(chunk: str, tags: List[str], api_key: str) -> str:
    """Тегирует чанк с помощью LLM."""
    # Реализация вызова LLM
    pass
```

Используйте в `rag_utils.py`:

```python
from llm_tagger import tag_with_llm

def tag_chunks(chunks: List[str], tags: List[str]) -> List[str]:
    results = []
    for chunk in chunks:
        tag = tag_with_llm(chunk, tags, os.getenv("LLM_API_KEY"))
        results.append(tag)
    return results
```

### Поддержка других векторных баз

Создайте адаптер в `vector_store.py`:

```python
class VectorStoreAdapter:
    def upload(self, chunks: List[dict]):
        """Абстрактный метод загрузки."""
        pass

class PineconeAdapter(VectorStoreAdapter):
    def upload(self, chunks: List[dict]):
        # Реализация для Pinecone
        pass

class WeaviateAdapter(VectorStoreAdapter):
    def upload(self, chunks: List[dict]):
        # Реализация для Weaviate
        pass
```

## 🐛 Отладка

### Включение детального логирования

В `main.py` измените уровень:

```python
logging.basicConfig(level=logging.DEBUG)
```

### Проверка работы парсера

```python
from rag_utils import extract_yaml_metadata, split_to_chunks

with open('docs/test.md') as f:
    content = f.read()
    
metadata, text = extract_yaml_metadata(content)
chunks = split_to_chunks(text)
print(f"Метадата: {metadata}")
print(f"Чанков: {len(chunks)}")
```

### Тестирование эмбеддингов

```python
from embedder import embed_chunk

embedding = embed_chunk("Test text")
print(f"Размерность: {len(embedding)}")
```

## 📊 Производительность

### Оптимизация

1. **Батчинг эмбеддингов:** OpenAI API поддерживает батчи до 2048 текстов
2. **Параллельная обработка:** Используйте `multiprocessing` для обработки файлов
3. **Кэширование:** Кэшируйте эмбеддинги для неизменённых чанков

### Оценка времени

- Парсинг 1 файла: ~0.1-0.5 сек
- Эмбеддинг 1 чанка: ~0.5-1 сек (зависит от API)
- Загрузка 100 чанков: ~1-2 сек

---

**Для дополнительных вопросов см. основной README.md**
