# Установка LEANN для auto-rag

LEANN может требовать специальной установки в зависимости от вашей системы и версии Python.

## 🔍 Проблема

Если при установке `pip install leann` вы получаете ошибку:
```
ERROR: Cannot install leann... because these package versions have conflicting dependencies.
The conflict is caused by: leann-backend-hnsw
```

Это означает, что пакет `leann-backend-hnsw` недоступен для вашей платформы/версии Python.

## ✅ Решения

### Решение 1: Установка через uv (рекомендуется)

LEANN официально рекомендует использовать `uv` для установки:

```bash
# Установите uv
pip install uv

# Установите LEANN
uv pip install leann
```

### Решение 2: Установка из исходников

```bash
# Клонируйте репозиторий
git clone https://github.com/yichuan-w/LEANN.git
cd LEANN

# Установите из исходников
pip install -e .
```

### Решение 3: Использование conda (если используете Anaconda)

```bash
conda install -c conda-forge leann
```

### Решение 4: Установка компонентов отдельно

Попробуйте установить компоненты по отдельности:

```bash
pip install leann-core
pip install leann-backend-hnsw
pip install leann
```

### Решение 5: Использование виртуального окружения

Создайте чистое виртуальное окружение:

```bash
python -m venv venv_leann
source venv_leann/bin/activate  # Linux/Mac
# или
venv_leann\Scripts\activate    # Windows

pip install --upgrade pip
pip install leann
```

## 🔄 Альтернатива: Использование Pinecone

Если установка LEANN не удаётся, вы можете использовать Pinecone:

```bash
# Установите Pinecone
pip install pinecone

# Используйте Pinecone в auto-rag
python main.py --backend pinecone
```

**Требования для Pinecone:**
- API ключи в `.env` файле
- Интернет-соединение

## 🧪 Проверка установки

После установки проверьте:

```bash
python -c "from leann import Builder, Searcher; print('✅ LEANN установлен!')"
```

Если команда выполняется без ошибок, LEANN готов к использованию.

## 📝 Использование в auto-rag

После успешной установки:

```bash
python main.py --backend leann --index-name my-docs
```

## ⚠️ Важно

LEANN является **опциональным** компонентом. Система auto-rag будет работать:
- ✅ С Pinecone (если установлен)
- ✅ В режиме dry-run (без векторной БД)
- ❌ С LEANN только если он успешно установлен

## 🐛 Если ничего не помогает

1. Проверьте версию Python: `python --version` (рекомендуется 3.8+)
2. Обновите pip: `pip install --upgrade pip`
3. Проверьте архитектуру системы (LEANN может не поддерживать некоторые платформы)
4. Используйте Pinecone как альтернативу

## 📚 Дополнительные ресурсы

- [Официальный репозиторий LEANN](https://github.com/yichuan-w/LEANN)
- [Issues LEANN на GitHub](https://github.com/yichuan-w/LEANN/issues) - если проблема специфична

---

**Примечание:** Если у вас возникли проблемы с установкой LEANN, система auto-rag продолжит работать с Pinecone или в режиме dry-run для тестирования парсинга.
